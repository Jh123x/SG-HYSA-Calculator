import React from "react"
import { Box, Typography } from "@mui/material"

export const Result = ({ results }) => {
    return <Box>
        <Typography variant="h2">Results Placeholder</Typography>
    </Box>
}
